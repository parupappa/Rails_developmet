<!--
<h1>2chクローン</h1>
<%= link_to "スレッド新規作成", "/boards/new" %>
<hr>
<% @boards.each do |board| %>
    <%= board.title %> <br>
<%= link_to board.title, "/boards/#{board.id}" %> </br>
<% end %>
-->
 <div class="wrapper wrapper__bg">
     <div class="container">
         <h1>俺の掲示板</h1>
         <%= link_to "スレッド新規作成",new_board_path %>
         <hr>
         <div class="panel">
             <% @boards.each do |board| %>
             <%= link_to board.title, board_path(board) %> </br>
             <% end %>
         </div>
     </div>
 </div>